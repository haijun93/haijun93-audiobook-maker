[tool.ruff]
target-version = "py311"
line-length = 120
extend-exclude = [
  ".cache",
  ".venv",
  ".venv311",
  ".venvs",
  ".work",
  "audiobooks",
  "korean_audiobook_runs",
]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "B"]
